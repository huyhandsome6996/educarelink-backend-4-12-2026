# Python
__pycache__/
*.py[cod]
*.pyo
venv/
env/
.env

# Django
db.sqlite3
*.log
staticfiles/
media/

# IDE
.vscode/
.idea/
*.swp

# Node / Expo (trong thư mục mobile/)
mobile/node_modules/
mobile/.expo/
mobile/dist/
mobile/web-build/

# Node (root level for docx generation)
node_modules/
bun.lock
package.json
# Exception: Tailwind CLI build tooling (frontend/tailwind-build/) needs its
# package.json tracked so the CSS bundles can be rebuilt deterministically.
!frontend/tailwind-build/package.json
!frontend/tailwind-build/package-lock.json

# Local QA dev artifacts (not for repo)
backend/settings_iso.py
run_dev_server.py
sitecustomize.py
